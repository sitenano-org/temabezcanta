// This layout is kept for backwards compatibility
// The main layout is now in app/[locale]/layout.tsx
export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}

